// import { createAction } from "@reduxjs/toolkit";

export const toggleLike = () => {
    return {
        type: 'TOGGLE_LIKE',
    };
};


/* export const user_photo = createAction(
    'user_photo',
    (obj) => ({
        payload: {
            photo: obj.photo,
        }
    })
); */